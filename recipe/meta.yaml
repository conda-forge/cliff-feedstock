{% set name = "cliff" %}
{% set version = "4.5.0" %}
{% set bundle = "tar.gz" %}
{% set hash = "61beeac238530beb3ae19eefb421b8b290b97dcc9efee01fc32fe62fb75a31d9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.{{ bundle }}
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ bundle }}
  sha256: {{ hash }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python

requirements:
  host:
    - python >=3.8
    - pip
    - pbr !=2.1.0,>=2.0.0

  run:
    - python >=3.8
    - autopage >=0.4.0
    - cmd2 >=1.0.0
    - importlib_metadata >=4.4
    - prettytable >=0.7.2
    - stevedore >=2.0.1
    - pyyaml >=3.12

test:
  imports:
    - cliff
    - cliff.app
    - cliff.commandmanager
    - cliff.command
    - cliff.interactive

about:
  home: https://github.com/openstack/cliff
  license: Apache-2.0
  license_family: APACHE
  license_file: LICENSE
  summary: Command Line Interface Formulation Framework
  dev_url: http://git.openstack.org/cgit/openstack/cliff
  doc_url: http://docs.openstack.org/developer/cliff

extra:
  recipe-maintainers:
    - anguslees
    - amatthies
    - tschoonj
